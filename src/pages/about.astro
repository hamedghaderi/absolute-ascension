---
import '../styles/global.css'

const pageTitle = 'About Me'

const identity = {
	firstName: 'Hamed',
	country: 'Iran',
	occupation: 'Web Developer',
	hobbies: ['Watching movies', 'Trining', 'Drink coffee']
}

const skills = ['HTML', 'CSS', 'JavaScript', 'React', 'Astro', 'Vue', 'PHP', 'Laravel']

const happy = true
const finished = false
const goal = 3
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{pageTitle}</title>
	</head>
	<body>
        <nav class="text-blue-600 py-2 container mx-auto">
			<a class="hover:text-blue-500 hover:underline" href="/">Home</a>
			<a class="hover:text-blue-500 hover:underline" href="/about/">About</a>
			<a class="hover:text-blue-500 hover:underline" href="/blog/">Blog</a>
		</nav>
		<div class="container mx-auto">
		<h1 class="text-4xl font-bold text-violet-700">{pageTitle}</h1>
		<p>Here are a few facts about me:</p>
		<ul>
			<li>My name is {identity.firstName}.</li>
			<li>I live in {identity.country} and I work as a {identity.occupation}.</li>
			{identity.hobbies.length >= 2 &&
				<li>Two of my hobbies are: {identity.hobbies[0]} and {identity.hobbies[1]}</li>
			}
		</ul>
		<p>My skills are:</p>
		<ul>
			{skills.map(skill => <li class="font-bold text-green-700">{skill}</li>)}
		</ul>
		{happy && <p>I am happy to be learning Astro!</p>}
		{finished && <p>I finished this tutorial!</p>}
		{goal === 3 ? <p>My goal is to finish in 3 days.</p> : <p>My goal is not 3 days.</p>}
		</div>
	</body>
</html>
